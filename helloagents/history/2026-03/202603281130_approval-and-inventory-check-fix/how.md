# How

1. 在采购与销售服务层增加统一的审批角色校验方法，并由控制器传入当前登录角色。
2. 修正前端订单列表、入库/出库页面与动态路由的权限标记，使采购员、销售员和库管按职责看到正确操作。
3. 将库存盘点页面改为先生成差异报告，再逐条调用 `/api/inventory-checks` 提交差异并刷新库存。
4. 通过前端构建、后端测试/打包与服务启动验证改动有效。
5. 同步更新知识库与历史索引，并迁移方案包到 `history/`。
